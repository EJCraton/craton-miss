<p id="notice"><%= notice %></p>

  <%= image_tag @archive.avatar.url(:medium) %>
  <%= link_to 'Download', @archive.avatar(:original), :"download"=>"Image#{@archive.id}" %>

<%= link_to 'Edit', edit_archive_path(@archive) %> |
<%= link_to 'Back', archives_path %>
